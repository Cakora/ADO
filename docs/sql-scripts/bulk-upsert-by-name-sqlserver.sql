-- Bulk upsert by name (SQL Server)
-- Batch size: 500
-- Note: This script intentionally avoids MERGE and CTE batching patterns.

-- Example stub (replace with your schema/table details):
-- Process 500 rows per batch from a staging table.
-- This is documentation-only; not executed by the library.
SELECT TOP (500) 1 AS BatchSizeMarker;

