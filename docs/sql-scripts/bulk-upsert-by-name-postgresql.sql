-- Bulk upsert by name (PostgreSQL)
-- Batch size: 500
-- Note: This script is documentation-only; not executed by the library.

-- Example stub (replace with your schema/table details):
-- 1) Bulk import your payload into a staging table (e.g., public.stage_items)
-- 2) Apply the upsert using INSERT ... ON CONFLICT

-- Example marker query:
SELECT 500 AS batch_size_marker;
